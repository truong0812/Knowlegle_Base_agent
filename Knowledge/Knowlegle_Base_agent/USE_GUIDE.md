# Knowledge Base: Knowlegle_Base_agent

> Auto-generated by **Knowledge Base Agent**

## Quick Start

```bash
# Analyze repository (graph-aware, static only)
python -m scripts.cli analyze --repo /path/to/project --out /path/to/project/.kb --skip-ai --with-graph --depth 2

# Validate
python -m scripts.cli validate --kb /path/to/project/.kb

# Query
python -m scripts.cli query "Parser làm gì?" --kb /path/to/project/.kb --with-graph
```

## Full Workflow

### 1. Scan & Parse

```bash
python -m scripts.cli scan --repo /path/to/project
python -m scripts.cli parse --repo /path/to/project
```

### 2. Analyze (Create Knowledge Base)

```bash
# Static analysis (fast, no LLM)
python -m scripts.cli analyze --repo /path/to/project --out /path/to/project/.kb --skip-ai --with-graph --depth 2

# With LLM enrichment (requires OPENAI_API_KEY)
python -m scripts.cli analyze --repo /path/to/project --out /path/to/project/.kb --with-graph --depth 2

# Skip AI for MEM layer only (save tokens on large repos)
python -m scripts.cli analyze --repo /path/to/project --out /path/to/project/.kb --with-graph --depth 2 --skip-mem-ai
```

### 3. Validate & Index

```bash
python -m scripts.cli validate --kb /path/to/project/.kb
python -m scripts.cli index --kb /path/to/project/.kb
```

### 4. Query

```bash
# Flat semantic search
python -m scripts.cli query "AuthService làm gì?" --kb /path/to/project/.kb

# Graph-aware retrieval (recommended)
python -m scripts.cli query "AuthService được gọi bởi những gì?" --kb /path/to/project/.kb --with-graph
```

### 5. Runtime Intelligence (Phase 4)

```bash
python -m scripts.cli ingest-telemetry traces.json --kb /path/to/project/.kb
python -m scripts.cli update-runtime-metadata --kb /path/to/project/.kb
python -m scripts.cli train-ranking-model --kb /path/to/project/.kb --min-samples 20
python -m scripts.cli auto-tune --kb /path/to/project/.kb
```

### 6. MCP Server (Phase 5)

```bash
# Start MCP server for AI coding agents
python -m scripts.cli serve --kb /path/to/project/.kb

# With auto-sync file watcher
python -m scripts.cli serve --kb /path/to/project/.kb --watch
```

### 7. Web Dashboard (Phase 5)

```bash
python -m scripts.cli dashboard --kb /path/to/project/.kb
```

### 8. Incremental Enrichment (Phase 5)

```bash
# Enrich only entries missing AI data
python -m scripts.cli enrich --kb /path/to/project/.kb

# Retry previously failed entries
python -m scripts.cli enrich --kb /path/to/project/.kb --retry-failed
```

## Multi-Repo Federation

```bash
# Analyze each repo separately
python -m scripts.cli analyze --repo /path/to/project-a --out /path/to/project-a/.kb --skip-ai --with-graph --depth 2
python -m scripts.cli analyze --repo /path/to/project-b --out /path/to/project-b/.kb --skip-ai --with-graph --depth 2

# Register and resolve cross-repo references
python -m scripts.cli add-repo /path/to/project-b/.kb --name project-b --kb /path/to/project-a/.kb
python -m scripts.cli resolve-cross-repo --kb /path/to/project-a/.kb
```

## Output Structure

```text
.kb/
  entries/                 # JSON materialized views: arch, mod, file, mem
  graph/                   # Symbol graph data
    nodes.jsonl            # One JSON object per symbol node
    edges.jsonl            # One JSON object per relationship edge
    adjacency.json         # Precomputed adjacency list
    features.jsonl         # Feature clusters
    hotpath.json           # Hot-path scores
    versions/              # Temporal snapshots
  index/                   # Retrieval index
    faiss.index            # FAISS vector index
    id_map.json            # Maps vector IDs to KB entry IDs
  telemetry/               # Runtime traces, feedback, tuning config
  manifest.json            # Snapshot metadata
  quality_report.json      # Validation results
```

## Supported Languages

- Python (tree-sitter)
- C# (tree-sitter)
- C++ (tree-sitter)

## CLI Commands Reference

| Command | Purpose |
|---------|---------|
| `scan` | List source files with language detection |
| `parse` | Extract symbols and imports from source |
| `analyze` | Run full analysis pipeline |
| `validate` | Check KB consistency and coverage |
| `index` | Build/rebuild FAISS vector index |
| `query` | Semantic or graph-aware query |
| `versions` | List stored graph versions |
| `diff` | Show diff between graph versions |
| `ingest-telemetry` | Ingest OpenTelemetry traces |
| `update-runtime-metadata` | Aggregate runtime metadata |
| `train-ranking-model` | Train retrieval tuning from feedback |
| `auto-tune` | Apply tuning overrides |
| `show-tuning-stats` | Show tuning statistics |
| `add-repo` | Register external repository |
| `resolve-cross-repo` | Resolve cross-repo references |
| `update-shared-deps` | Update shared dependencies |
| `serve` | Start MCP server |
| `enrich` | Incremental LLM enrichment |
| `dashboard` | Launch web dashboard |
| `graph-health` | Show graph health metrics |
| `query-analytics` | Show retrieval analytics |

---

*This file was auto-generated. Edit freely.*
