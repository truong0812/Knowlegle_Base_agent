# Knowledge Base: Knowlegle_Base_agent

> Auto-generated by **Project Understanding System** on 2026-05-14

## Structure

```
Knowlegle_Base_agent/
├── snapshots/           # Historical snapshots by date
│   └── YYYY-MM-DD/
│       └── snapshot.json
├── indexes/             # Semantic search indexes
│   └── semantic_index.json
├── latest.json          # Current snapshot (copy of newest)
├── metadata.json        # Project metadata & stats
└── USE_GUIDE.md         # This file
```

## Statistics

| Metric | Count |
|--------|-------|
| Files | 276 |
| Symbols | 430 |
| Relations | 1414 |
| Summaries | 706 |

**Last commit:** `e0eff8bb5673` on branch `main`

**Top file types:** `.json` (214), `.py` (53), `.md` (5), `.gitignore` (1), `.txt` (1)

**Top symbol kinds:** `method` (250), `class` (80), `function` (51), `constant` (44), `constructor` (2)

## How to Update

```bash
# Basic ingestion (heuristic summaries)
python -m project_understanding.cli.main ingest /path/to/Knowlegle_Base_agent --project-name Knowlegle_Base_agent

# With LLM summaries (recommended)
python -m project_understanding.cli.main ingest /path/to/Knowlegle_Base_agent --project-name Knowlegle_Base_agent

# Skip enrichment for faster results
python -m project_understanding.cli.main ingest /path/to/Knowlegle_Base_agent --project-name Knowlegle_Base_agent --no-enrichment
```

## Query Examples

```bash
# Get context for a specific file
python -m project_understanding.cli.main query file "src/api/auth.ts" --repo Knowlegle_Base_agent --profile review-agent

# Search for a symbol
python -m project_understanding.cli.main query symbol "LoginRequest" --repo Knowlegle_Base_agent --profile dev-agent

# Semantic search
python -m project_understanding.cli.main query semantic "authentication flow" --repo Knowlegle_Base_agent --profile review-agent

# Change impact analysis
python -m project_understanding.cli.main query changes "src/api/auth.ts" "src/utils/token.ts" --repo Knowlegle_Base_agent --profile review-agent
```

## Agent Profiles

| Profile | Use Case |
|---------|----------|
| `review-agent` | Code review, PR analysis |
| `dev-agent` | Development assistance, debugging |
| `doc-agent` | Documentation generation |

## Output Format

Each query returns a **ContextBundle** containing:
- **Items**: Files, symbols, modules relevant to the query
- **Relations**: Dependencies and call graphs
- **Summaries**: LLM-generated or heuristic descriptions
- **Conventions**: Detected coding patterns
- **Risks**: Identified risk areas

---

*This file was auto-generated. Edit freely — it won't be overwritten on re-ingest.*
